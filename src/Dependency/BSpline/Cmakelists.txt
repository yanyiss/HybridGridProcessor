cmake_minimum_required(VERSION 3.10)
#project(BSpline)
add_definitions(/D_USE_MATH_DEFINES)

set(LIBRARY_OUTPUT_PATH "${PROJECT_BINARY_DIR}/lib")
aux_source_directory(${PROJECT_SOURCE_DIR}/src/Dependency/BSpline BSpline_src)
include_directories("${PROJECT_SOURCE_DIR}/src/Dependency/BSpline")
add_library(BSpline STATIC ${BSpline_src})


SET(EIGEN_PATH "D:/OpenSource/eigen")
include_directories(${EIGEN_PATH})
SET(OPENMESH_PATH "D:/OpenSource/OpenMesh/include")
include_directories(${OPENMESH_PATH})
SET(OPENMESH_LIB_PATH "D:/OpenSource/OpenMesh/lib")